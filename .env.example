# Environment Variables for Crazy 8 Game

# PostgreSQL Database URL (from Render PostgreSQL)
# Format: postgres://username:password@hostname:port/database
# Render provides this automatically as DATABASE_URL when you create a PostgreSQL instance
DATABASE_URL=

# Server Port (optional, defaults to 3001 locally, Render provides PORT automatically)
PORT=3001

# Node Environment
NODE_ENV=development
